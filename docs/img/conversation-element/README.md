# The conversation element

`thread-phone.png` (390 px) and `thread-wide.png` (1280 px): an open thread in
the conversations card, rendered by `<retinue-conversation>` against a mocked
gateway — the bar with back, title, model picker, autoplay and archive; a
reply with a quoted draft and a code block, each with its copy button;
click-to-fill chips; an attachment row; the composer with mic, attach and send.
