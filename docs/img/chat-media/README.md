# Chat media kinds

`kinds.png` — the chat page (390 px, mocked `/chats` responses) showing every
media kind the gateways now keep: an image, a voice note above its transcript,
a video with a reserved box, a document row (the gateway-stated name and size,
a link that opens the file), an animated sticker as a file row, and a record
older than the metadata statements (a bare "Attachment" link).
